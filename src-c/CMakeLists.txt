set(INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/include")
set(SOURCE_CODE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/src")

set(HELPERS_LIB "helpers")

add_subdirectory(${INCLUDE_DIR})
add_subdirectory(${SOURCE_CODE_DIR})